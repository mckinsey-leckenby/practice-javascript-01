const car = {
    name: "Chevy",
    color: "Red",
    make: "Silverado",
    model: "2021"
}

let {name, color, make, model} = car 

console.log(make)

let arr = [1,2,3,4,5]
let [a,b,c,d,e] = arr 

console.log(d)